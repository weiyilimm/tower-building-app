LoginDeleteUser_HTTPResponseDeleteUserByID_NoUnexpectedISE
	returns 500 when the correct format but no existant user is searched for. should return 404.
	Print(userToDelete) triggers try catch if usertodelete is null

getFriends_HttpResponseGetsFriendsList_FriendsListIsTheSameAsExpectedNoContentIfEmpty
	returns 404 not found if a user has an empty friends list. should probably return 204 no content.
	
	once fixed will erorr again. if the ID is null. still returns 404, 
	but to be inlien with other responses should return 500 internal server error
	

getFriends_HttpResponseGetFriendsWithNoUser_IfUserOkIfNoUserNotFoundIfNullISE
	if user id passed in is null, should probably return error 500, 
	as that is the format across the rest of the controllers

updateUserBuilding_HttpResponseUpdateBuildingsWithIDandJSONData_ModelExistOkElseCreatedIfNoUserOrBadInputISE
	try catch for badly formatted json data.
	when the data is parsed to a hash map, if the item is not what is expected it will error.

	add try catch to json parser and throw error 500 if parsing data fails

getUserBuildings_HttpResponseGetUserBuildings_CorrectAmmountReturnedOkIfBuildingsISEIfNull
	if user has no buildings, the program still trys to iterator through a list.
	add an "if empty" or try catch.